<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.Dao.UserDao">
    <sql id="BASE_TABLE">
        tb_user
    </sql>

    <sql id="BASE_COLUMN">
        user_id,user_name,user_age
    </sql>

    <!--因为mysql的表字段和User对象的字段不一致，所以需要转换匹配-->
    <resultMap id="BaseResultMap" type="com.example.demo.Dao.User">
        <result column="user_id" property="id" jdbcType="INTEGER"/>
        <result column="user_name" property="name" jdbcType="VARCHAR"/>
        <result column="user_age" property="age" jdbcType="INTEGER"/>
    </resultMap>

    <insert id="add" parameterType="com.example.demo.Dao.User">
        insert into
        tb_user(user_id,user_name,user_age)
        values(#{id,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{age,jdbcType=INTEGER})
    </insert>

    <!--resultMap 是将返回的结果进行mapping匹配，如果mysql和User的字段一致，那么可以直接使用 resultType="User" 直接返回-->
    <select id="searchById" parameterType="int" resultMap="BaseResultMap">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
        WHERE user_id = #{id,jdbcType=INTEGER}
    </select>

    <select id="searchAll" resultMap="BaseResultMap">
        SELECT
        <include refid="BASE_COLUMN"/>
        FROM
        <include refid="BASE_TABLE"/>
    </select>
</mapper>